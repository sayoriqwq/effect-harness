{
  "name": "@sayoriqwq/effect-harness",
  "type": "module",
  "version": "0.0.0",
  "private": false,
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "pnpm@10.33.2",
  "description": "Shared Effect v4 beta harness for local target projects.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sayoriqwq/effect-harness"
  },
  "main": "dist/bin/effect-harness.js",
  "bin": {
    "effect-harness": "dist/bin/effect-harness.js"
  },
  "files": [
    "HARNESS.md",
    "README.md",
    "dist",
    "harness",
    "repos",
    "repos/effect.subtree.json"
  ],
  "engines": {
    "node": ">=22.22.1"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "prepack": "pnpm build",
    "dev": "pnpm build && node dist/bin/effect-harness.js --help",
    "effect:patch": "effect-tsgo patch",
    "effect:setup": "effect-tsgo setup",
    "effect:verify": "node bin/effect-harness.ts provider-verify --harness .",
    "knip": "knip",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "prepare": "effect-tsgo patch",
    "source:status": "node ../partita/dist/bin/partita.js source status --root . --contract repos/effect.subtree.json --name effect",
    "source:update": "node ../partita/dist/bin/partita.js source update --root . --contract repos/effect.subtree.json --name effect --dry-run",
    "source:verify": "node ../partita/dist/bin/partita.js source verify --root . --contract repos/effect.subtree.json --name effect",
    "test": "vitest run tests/*.test.ts",
    "typecheck": "tsgo --noEmit",
    "typecheck:tsc": "tsc --noEmit",
    "verify": "pnpm effect:verify && pnpm typecheck && pnpm test && pnpm lint && pnpm knip"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@effect/language-service": "catalog:",
    "@effect/tsgo": "catalog:",
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "eslint": "catalog:",
    "knip": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
